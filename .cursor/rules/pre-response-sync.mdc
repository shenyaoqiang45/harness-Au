---
description: 回答前先拉取远端代码并检查事件 checklist
alwaysApply: true
---

# 回答前同步与 Checklist 检查

每次收到用户消息后、开始实质性回答或改代码之前，**必须先**完成以下两步：

## 1. 拉取远端最新代码

```bash
git fetch origin
git pull origin
```

- 若 pull 失败（冲突、未提交改动等），先向用户说明阻塞原因，再继续或请用户处理。
- 若工作区有未提交改动，不要丢弃；pull 后如有冲突需说明。

## 2. 检查 `docs/events/checklist.md`

读取该文件，结合当前日期关注：

- **今日 / 已过期待办**：`[ ]` 且日期 ≤ 今天的事件
- **近期 P0**：7 日内未完成的 P0 事件
- **用户问题相关**：若问题涉及宏观、FOMC、CPI、沃什因子、报告跑批，对照清单中的「发布后动作」

## 回答时的处理

- 若 checklist 有与当前任务相关的待办，在回答开头**简要提示**（1–3 句），例如逾期 P0、今日应执行的 `cli run`。
- 若用户明确要求执行清单动作（fetch / report / 改配置），按清单步骤执行，完成后在 checklist 中勾选并写备注。
- 无相关待办时不必展开 checklist，继续正常回答。
- **出报告硬闸门**不在本文件：见 `pre-report-checklist.mdc`（`run`/`report` 前必须过清单）。

## 参考

- 事件分级与标准命令见 `docs/adr/0001-event-driven-report-checklist.md`
- Checklist 路径：`docs/events/checklist.md`
- 出报告前闸门：`.cursor/rules/pre-report-checklist.mdc`
